{
  "name": "fieldcraft",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": ["apps/*"],
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "dev": "turbo run dev",
    "eval": "turbo run eval",
    "eval:live": "turbo run eval:live",
    "test": "turbo run test",
    "doctor": "turbo run doctor"
  },
  "devDependencies": {
    "turbo": "2.10.4"
  }
}
